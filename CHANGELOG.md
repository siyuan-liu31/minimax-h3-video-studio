# Changelog

记录 MiniMax H3 Video Studio 面向用户和运维的重要变化。提交明细仍以 Git 为准；这里只保留会影响使用、兼容、数据或部署的内容。

## [Unreleased]

## [0.6.1-dev.20260925.2] - 2026-09-25

开发机验收版本；未创建正式 GitHub Release。

- 改歌词使用独立的选歌、识别、原词/新词对照编辑与首段试听流程；按歌曲保存浏览器草稿，异步识别不覆盖手动修改。
- 生成结果提供原曲与结果播放器和同位置切换；保留干声/伴奏试听下载。
- SoulX 生成人声先匹配原唱 RMS（增益限制 ±12 dB）再混音；已有分轨结果支持 CPU 重新混音、人声/伴奏独立调节、削波保护，保留原始输出；重混保留伴奏双声道与采样率。
- CLI 支持 `voice rewrite --preview`，Agent operation 增加 `voice.transcribe` / `voice.remix`，下载支持 `remix` 音轨。

## [0.6.1-dev.20260925.1] - 2026-09-25

开发机验收版本；未创建正式 GitHub Release。

### Added

- SoulX 中文保留旋律改词：CLI `voice rewrite`、持久后端任务及侧栏歌词编辑；支持原歌词校正、可选参考音色、采样参数复用，输出混音、新唱干声及原伴奏。

## [0.6.1-dev.20260925] - 2026-09-25

开发机验收版本；未创建正式 GitHub Release。

### Added

- 复刻说明支持输入 `@` 搜索并插入图片素材标签，选中时同步加入替换参考，规划前检查引用是否仍已连接。
- 抖音侧栏自动连接 Mac 本机导入服务：在本机解析/下载后直接上传 Studio 资产库；安装脚本支持当前会话启动和在授予 macOS 磁盘权限后的登录自启，浏览器和开发机均不接收抖音 Cookie。

### Fixed

- 连续插入多个 `@` 素材时保持输入光标位置，避免后选素材标签跳到说明开头。
- 前端检测到本机导入能力时优先走本机 Chrome 会话，避免开发机无会话导致的反复 403；本机可重试失败的缓存缩短至一分钟，并明确说明“需要新 Cookie”不一定是登录失效，提供本地文件上传回退；ESLint 跳过本机临时验收目录。
- 抖音导入失败时，前端清除过期的“正在下载”进度，显示失败阶段、原因、下一步和诊断码；CLI 与开发机任务分别标识 HTTP 403/429，避免把拒绝请求误报为确定的 Cookie 失效。

## [0.6.1-dev.20260924] - 2026-09-24

开发机验收版本；未创建正式 GitHub Release。

### Fixed

- 24 FPS 归一化视频在创建复刻项目时使用实际视频帧数，避免 AAC 音频尾部使容器时长略长、误判分镜帧数不匹配。
- ComfyUI 结果收集排除输入素材和临时预览，修复新版 LoadVideo 回执被误当作生成结果、导致复刻分段回收失败的问题。
- 复刻分段按 Motion Context 实际裁头帧数计算有效长度，并在来源参考中加入对应重叠帧，修复多段成片变短；旧的不含重叠记账的多段方案在运行前要求重新规划。

### Added

- Studio 抖音素材入口：分享文本解析、清晰度选择、持久后台下载、取消/重试、资产去重导入、预览与复刻跳转；服务端有界运行 yt-dlp，复用认证、Origin、媒体验证和配额。
- CLI 新增抖音远端导入/解析、任务查询/等待/取消/重试及对应 Agent operations；`import --local` 支持本机浏览器会话下载后上传视频。旧本地命令保持兼容。
- 复刻工坊新增服务端项目恢复、素材/审阅/成片三步工作流、逐段提示词编辑、来源与结果对照、连续依赖重跑范围提示、JSON 导出及指定项目跳转长视频精修；保存草稿不再自动提交生成。
- 新增 `h3ctl replication` 分阶段命令与五个 Agent operations：保存草稿、检查/导出、带版本冲突保护的逐段修改，以及复用原项目的生成/合并/下载恢复。
- Studio 新增原生“复刻工坊”：输入来源视频、复刻说明、保留项、替换目标和可选图片参考，先做镜头分析并编译为版本化 `h3.replication/v1` 配方，再复用持久长视频项目逐段生成、合并和精确裁帧；支持原声保留、分段参考原声、重新生成音频和静音成片。
- `h3ctl video replicate` 与 Agent operations `video.replication.plan` / `video.replication.produce` 支持同一复刻合同，可使用本地文件或远端 locator、仅规划、后台执行、断点恢复和原子下载。

### Changed

- 长视频编辑器保存时保留复刻 recipe；只读提交模式保留分段提示词原始文本。复刻逐段 PATCH 在原子版本校验后失效当前段及依赖段，保留其他已完成结果。
- 复刻工坊取消来源视频的 15–60 秒限制：短视频内部补帧后裁回原帧数，长视频自动分段；复刻项目不再受 200 个分镜切点和 1,000 段限制，改由可配置的项目 JSON 容量约束。

- 长视频项目现在可校验、保存和执行复刻配方，并把来源与替换参考的 SHA-256 纳入资产删除保护和运行前完整性检查；复刻成片在合并后裁到来源的精确 24 FPS 帧数。

## [0.6.0] - 2026-09-22

### Added

- 新增 Qwen-Image 2.1 BF16 原生 ComfyUI Profile：统一支持文生图与 1–10 张有序参考图的指令编辑，原生 2K 预设、40 步 / CFG 1 默认值、精确 BF16 主模型/编码器/VAE 能力检测；CLI、Agent operation 和前端共用受控工作流，不静默量化或降低尺寸。
- YingMusic-SVC 歌曲换声可选保留换声干声与原伴奏，并分别试听/导出最终混音、干声和伴奏；回声与混响可独立开关。前端试听、浏览器下载和 CLI 分轨下载读取同一持久音轨，旧任务与默认成品保持兼容。
- YingMusic-SVC 歌曲换声开放采样步数、引导强度与随机种子调整，前端、CLI 和 Agent operation 均可设置；每次任务记录实际种子以便复用参数或多次抽卡。
- 换声工作区新增浏览器话筒录音：用户可录制、试听并明确选择上传为原音频或参考音频，自动转换为现有服务端接受的 WAV；支持取消、权限错误提示和关闭面板时安全释放话筒。
- Studio 左侧新增换声工作区：Vevo2 FM-only 与 YingMusic-SVC 模式选择、原音频/参考音频拖拽或文件选择上传、现有音频资产复用、可用性提示、持久任务进度/排队/取消/删除，以及完成 WAV 的试听和下载；英文界面同步支持。
- 新增 Agent 友好的 `h3ctl video migrate-character`：支持本地与远端 locator、实用上不限时长的 24 FPS 源视频分窗、Base/Turbo 可配步数、Motion Context 音画连续、精确裁帧拼接、四种音频策略、断点恢复和存储预检。
- 新增版本化 `video.character_migration` 能力与严格 Draft 2020-12 Agent operations `video.character_migration.plan`、`video.character_migration.produce`、`media.mux_audio`；服务端新增通用原子音频置换接口。
- 新增项目内置 H3 生产 Skill 套件：人物对白生视频、舞蹈复刻与不限时长人物迁移，共享既有提示词编译器，并固化分段验收、尾段向前回填、稳定资产别名、断点恢复和成片交付流程。

### Changed

- README 与图片/CLI 文档覆盖视频、图片、换声及 Agent 直接操作的实际命令，并明确 Qwen-Image 2.1 的非商业 Qwen Research License；前端图片尺寸随 Qwen 2.1 Profile 选择原生 2K 预设，旧 Profile 尺寸不变。
- 长视频项目可选持久化 `h3.character-migration/v1` recipe 和源/角色 SHA-256；尾窗优先向前扩展为更大的合法重叠以减少或消除补帧，再执行终片精确裁切与音频后处理。旧 recipe、普通生成、`video compose`、现有项目和媒体合同保持不变。
- H3 Ref2VA 参考合同按官方规格更新为图片最多 9、视频最多 3、音频最多 3、混合文件合计最多 12；CLI、Agent operation schema、画布和长视频规划保持一致。旧 Ref2VA profile 身份仍可用于已有项目和断点恢复。

### Fixed

- `npm test` 在项目最低支持的 Node.js 22.13 上显式启用 TypeScript 类型剥离，避免前端测试导入 `.ts` 文件时报未知扩展名。
- 换声 Worker 加载模型或推理时，前端能力查询不再等待同一把长任务锁，避免换声面板长时间停留在“检查模型可用性”。

## [0.5.0] - 2026-09-04

### Added

- Studio 前端新增持久化的 English / 中文切换，首次访问默认英文；界面翻译仅作用于登记的产品文案，不改写用户 Prompt。
- 长视频项目新增 Motion Context latent 续接，支持 Base 与 Turbo LoRA、可配步数/上下文窗口、音画自动裁头、独立存储配额与完整性校验。
- Go CLI 新增 `h3ctl video compose`、`video trim`、`video concat` 和 Agent operation `video.compose`，可从项目 Spec 一键完成 Profile 锁定、顺序生成、裁头、拼接与原子下载。

### Fixed

- 生产网关将 CLI/SSH 兼容健康检查路径 `/health` 正确路由到 Python API，避免 `h3ctl doctor` 被前端服务误处理为 500。
- 竖屏视频在画布与资产库缩略图、实际播放中保持完整方向，不再被横屏容器裁切；资产库视频卡片按需加载并直接播放，提供与真实播放状态同步的播放/暂停按钮，同时补齐资产状态、参考帧率与远程恢复提示的英文翻译。
- 画布连线改为跟踪节点端口的实际布局中心；资产工具展开、文案换行或节点高度变化后，曲线起止点不再停留在固定高度计算出的旧位置。
- 结果抽屉的普通生成视频和剪辑派生视频缩略图均支持直接点击播放，并提供与媒体真实状态同步的中央播放/暂停按钮；资产卡片与结果卡片不再同时显示重复的浏览器原生播放按钮，点击视频画面也可切换状态。资产库和结果抽屉中的音频同样支持播放或暂停，且均保持按需加载；“在画布预览”保留为独立操作。

## [0.4.1] - 2026-09-02

### Added

- `h3ctl` 新增纯本地 `douyin parse|download|serve` 能力：通过 `yt-dlp` 解析或下载用户授权的抖音媒体，可显式读取本机浏览器会话；内置回环异步 API、Swagger/OpenAPI 文档、限时 token 下载、Range、限流、去重缓存与 SSRF 防护，不触发原有 H3 SSH context。
- 新增 H3 专用 `H3StudioSaveLatent` / `H3StudioLoadLatent` ComfyUI 节点，支持视频/音频 `NestedTensor` 检查点的版本化保存与恢复。

### Changed

- 项目对外品牌更名为 `MiniMax H3 Video Studio`，GitHub 默认 README 改为英文并保留中、日文入口；CLI `h3ctl`、`H3_STUDIO_*` 环境变量、数据路径、API 合同和持久化键保持兼容。
- H3 Base 默认改为 Direct Profile，优先保存成片；续采改为显式 `-base-resumable` Profile，检查点只在 `SaveVideo` 落盘后 best-effort 保存，失败不再将已生成视频判为任务失败。

## [0.4.0] - 2026-08-31

### Added

- 新增 CLI/Agent 音色转换：Amphion Vevo2 FM-only 支持语音/演唱的参考音色替换，YingMusic-SVC 支持完整歌曲的人声分离、转换与伴奏重混，包含持久任务、进度、取消和下载回执。
- 新增统一 GPU 资源管理器：ComfyUI 和换声 Worker 共享单卡独占 FIFO 租约，跟踪模型驻留、队列原因、显存状态、空闲释放与崩溃/取消回收。

### Changed

- ComfyUI 普通生成与长视频分段改由统一资源队列调度；不修改任务的模型精度、分辨率或采样参数。

## [0.3.1] - 2026-08-31

### Fixed

- 参考媒体探测改用 ffprobe 4.4 兼容的完整 stream/format 查询，避免旧版 ffprobe 因不支持 `stream_side_data` 精确选择器而拒绝所有视频上传与预处理。

## [0.3.0] - 2026-08-31

### Added

- 新增 H3 参考视频低 Token 预处理：保持方向和宽高比，输出 24 FPS、最长 15 秒、32 像素对齐的 H.264/YUV420P 派生视频，并提供幂等、可审计回执及 `h3ctl media prepare-reference` 原子能力；耗时处理采用可恢复后台任务，前端与 CLI 均可查看进度、取消和重试。
- 新增 `sm120 + SageAttention` 长序列风险预检；命中版本化安全策略时，服务端自动物化内部派生参考并在任务与工作流证据中记录原始/派生映射。
- H3 Base Profile 新增真正的 latent/sigma 断点续采，支持 `POST /api/jobs/:id/resume`、`h3ctl job resume`、前端任意追加步数、等待和下载。每条链仅保留最新检查点，默认 48 小时过期并定期 GC。

### Changed

- `minimax-h3-fl2va-base` 与 `minimax-h3-ref2va-base` Profile 升级到 `1.1`，能力检测新增 `SplitSigmas`、`SaveLatent`、`LoadLatent` 和 `DisableNoise` 节点要求；Turbo Profile 仍明确不开放续采。
- 参考视频探测回执新增容器旋转信息；能力回执新增版本化 H3 参考安全策略。

## [0.2.0] - 2026-08-29

### Added

- 新增 Go 客户端 `h3ctl`，为 Agent 提供资产上下载、图像/视频生成、可断线恢复的任务等待、首尾帧与媒体派生、长视频项目以及版本化原子操作合同。
- 新增本地/开发机 context、统一资源 locator、流式 multipart 上传和 `.part` 原子下载。CLI context 可选 direct URL 或自动生命周期的临时 SSH 端口转发，支持稳定 SSH alias 和租用机地址更新。
- 媒体派生结果可物化为不污染资产库的 internal asset，需用户显式保存才转为 library asset。

### Fixed

- 强化 CLI 的任意位置 flag、结构化错误、提交断线幂等恢复、重定向凭据隔离、长传输超时和并发下载原子性。
- 严格校验生成回执与所有服务端资源 ID，修复 operation Draft 2020-12 schema 合法性、跨平台 context 文件锁及旧 context 的敏感 URL 展示风险。
- Go CLI 移除 API key 参数、环境变量和请求头注入；旧 `api_key_env` 配置可安全读取，下次保存时自然清理。服务端可选鉴权与 Web 网关行为不变。
- CLI 连接改为按命令懒加载；本地 help/schema/usage 不受离线 SSH context 影响。SSH ready 改用私有 ControlMaster 的 check/forward 确定性握手，再严格验证 H3 health JSON；关闭、reap 与控制目录清理在成功输出前完成且有界。context 同名新增返回冲突，并支持显式清除保存的 SSH port。
- SSH ControlMaster 的 check/forward/exit 隔离用户 SSH 转发配置，启动取消与截止时间返回稳定错误；H3 health 现严格限定 JSON MIME 与 64 KiB 响应。

## [0.1.5] - 2026-08-29

### Documentation

- 新增完整的英文版与日文版 README，并在三个版本之间提供语言切换入口。
- 三种语言统一保留产品能力、安装运行、安全边界、截图与生成效果动图说明。

## [0.1.4] - 2026-08-23

### Documentation

- 将生成效果预览改为更轻量的仓库内 GIF，并使用 GitHub 原生 Markdown 图片语法，修复外部 Raw 图片地址加载失败时首页留白的问题。

## [0.1.3] - 2026-08-23

### Documentation

- 修复 GitHub 仓库首页不显示生成效果动图的问题：进一步压缩预览文件，并改用版本 Tag 固定地址避免分支缓存失效。

## [0.1.2] - 2026-08-23

### Documentation

- 将 README 的 5 秒生成视频动图压缩到 GitHub 首页可稳定加载的体积，同时保持清晰画面与完整动画预览。

## [0.1.1] - 2026-08-23

### Documentation

- README 的模糊生成封面替换为源视频有效画面开始后的连续 5 秒高清动图；完整视频仍不进入仓库。

## [0.1.0] - 2026-08-23

### Documentation

- README 改为图文产品导览，使用经用户授权的界面与生成封面示例，补充节点画布、七种视频模式、多模型生图、长视频分段续接、资产/结果管理和三步启动说明。
- 项目附带单一 `h3-ref2va-prompt-compiler` 本地 skill。
- 新增 SemVer、`main`、Tag、GitHub Release 与 CHANGELOG 一致性检查清单，并明确 GitHub 发布必须由用户当次授权。

### Security

- GitHub 发布版本排除私有验收证据、原始用户素材、完整生成结果、模型权重与部署包；仅对白名单中的 README 演示截图开放跟踪。
- 示例配置移除开发机绝对路径，API Key 只保留明确的占位符。

## [2026-08-23.5] - 2026-08-23

### Added

- 生成结果与剪辑派生结果支持混合多选、全选当前项和批量删除。
- 资产、生成结果与剪辑派生结果支持服务端持久化置顶，旧结果跨分页刷新后仍保留在结果顶部。
- 资产文件夹支持安全删除：只删除文件夹，资产和子文件夹自动移动到上一级，不会误删媒体文件。

### Documentation

- 新增从空机器准备 H3 Studio 的安装与运行指南，明确安装脚本边界、最小/完整依赖、ComfyUI/KJNodes、H3 模型目录、能力检查、SSH 隧道和常见排错。

## [2026-08-23.4] - 2026-08-23

### Fixed

- 视频剪辑、抽帧与音轨派生会在来源旁创建新节点并持久化到结果抽屉，不再自动进入资产库。
- 未保存的图像、视频等结果节点可通过右键菜单显式保存到资产；删除画布节点不再误删结果。
- 画布拖放只接受本地文件，节点内的图片和视频不再触发绿色导入框或重复上传。

## [2026-08-23.3] - 2026-08-23

### Fixed

- 本地选择或拖入完全相同的素材时，服务端按 SHA-256 复用已有资产，不再重复复制文件和创建资产 ID。
- 前端在命中重复素材时明确提示已复用；历史重复项仍可通过多选批量清理。

## [2026-08-23.2] - 2026-08-23

### Fixed

- 结果卡片的视频播放图标恢复在缩略图中央，不再被预览文字样式挤到卡片左侧。
- 视频缩略图改为从开场采样窗口选择代表帧，并自动淘汰旧的纯黑首帧缓存。
- 资产库按内容指纹收起重复素材，并增加多选、全选重复项和批量删除。

## [2026-08-23.1] - 2026-08-23

### Fixed

- 结果历史按当前服务端数据集身份恢复，旧画布与克隆实例的任务不再污染当前结果或导致列表突然只剩一项。
- 结果首页、深分页和空尾页使用权威窗口对账，并补齐慢网、超时、重试和删除竞态隔离。

### Changed

- 结果缩略图改为懒加载，视频预览显示缓冲/重试状态，大文件下载改为可见进度、可取消的流式传输。
- 服务端任务列表增加进程内缓存与深拷贝隔离，媒体响应继续支持 ETag、HEAD 和 Range。

## [2026-08-22.4] - 2026-08-22

### Changed

- 结果历史改为轻量、仅结果查询，并增加短期浏览器缓存，减少打开结果抽屉时的重复传输与等待。
- 预览、下载和缩略图增加条件缓存、字节范围与并发去重支持，改善刚生成视频的加载和历史结果浏览。
- 新增精简版 `AGENTS.md` 与 `docs/llm-wiki.md` 代码地图，后续开发按风险验证，不再固定要求五轮。

## [2026-08-22.3] - 2026-08-22

### Added

- 长视频时间线可导入现有媒体，并统一监视器、播放头、分镜时间线和完整序列视图。
- 长视频支持更可靠的播放、片段选择、删除和刷新恢复。

### Fixed

- 结果删除改为即时 UI 更新，并避免轮询把已删除任务重新放回列表。
- 结果卡片操作区缩小为更合理的响应式布局。

对应提交：`a5eaf43`–`ecd8058`。

## [2026-08-22.2] - 2026-08-22

### Added

- ComfyUI 按实际 H3 模型组件管理资源键，并在队列空闲后安全释放模型缓存。

### Fixed

- 新生成视频无需刷新页面即可重新装载和播放，并保留严格的原始预览 URL。
- 恢复结果视频缩略图，压缩结果操作按钮占用空间。

对应提交：`4f27309`–`989aa90`。

## [2026-08-22.1] - 2026-08-22

### Added

- 无限画布与多画布标签工作区，支持独立保存、切换和恢复。
- 画布支持 H3 Director 的 T2V、I2V、FL2V、R2V、V2V、RV2V 模式及可审计工作流合同。
- 长视频加入上一段尾帧/视频续接区间、画布与故事板同步控制，以及更完整的分镜工作区。

### Changed

- 视频 Prompt 采用只读提交语义：只解析素材标签，不自动翻译、扩写或重组正文。

对应提交：`851bec5`–`bc82b00`。
